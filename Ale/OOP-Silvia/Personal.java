class Personal {
    // Método con un parámetro
    double calcularPago(double sueldo) {
        return sueldo;
    }

    // Método sobrecargado con dos parámetros
    double calcularPago(double sueldo, double bono) {
        return sueldo + bono;
    }
}